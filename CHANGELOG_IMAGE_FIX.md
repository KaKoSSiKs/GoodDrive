# Changelog - Исправление отображения изображений

## Дата: 2025-11-10

### 🐛 Исправлено

**Проблема:** Изображения товаров не отображались в корзине и на странице оформления заказа.

**Решение:** Добавлена автоматическая конвертация относительных URL изображений в абсолютные.

### ✨ Изменения

#### Новые утилиты

- **`imageUtils.getAbsoluteUrl()`** - преобразует относительные URL изображений в абсолютные
- Автоматическая миграция существующих товаров в корзине

#### Обновлённые компоненты

1. **Корзина** (`frontend/src/routes/cart/+page.svelte`)
   - Изображения теперь загружаются с полным URL

2. **Страница оформления заказа** (`frontend/src/routes/checkout/+page.svelte`)
   - Изображения в сводке заказа отображаются корректно

3. **Карточки товаров** (`frontend/src/lib/components/PartCard.svelte`)
   - Используют новую утилиту для изображений

4. **Страница товара** (`frontend/src/routes/product/[id]/+page.svelte`)
   - Все изображения товара отображаются корректно

5. **Поиск** (`frontend/src/lib/components/SearchAutocomplete.svelte`)
   - Миниатюры в результатах поиска работают

#### Конфигурация

- Исправлена переменная `VITE_API_URL` в `docker-compose.yml`
- Добавлена документация по настройке в `documentation/image_fix_guide.md`

### 🚀 Запуск после обновления

1. **Если используете Docker:**
   ```bash
   docker-compose down
   docker-compose up --build
   ```

2. **Если запускаете локально:**
   ```bash
   # Создайте файл frontend/.env
   echo "VITE_API_URL=http://localhost:8000" > frontend/.env
   
   # Перезапустите фронтенд
   cd frontend
   npm run dev
   ```

3. **Очистите кэш:**
   - Откройте консоль браузера (F12)
   - Application → Local Storage → Очистите `gooddrive_cart`
   - Перезагрузите страницу (Ctrl+F5)

### 📝 Дополнительная информация

Подробную информацию о внесённых изменениях и настройке смотрите в:
- `documentation/image_fix_guide.md`

### 🔄 Обратная совместимость

✅ Все существующие товары в корзине будут автоматически обновлены при следующей загрузке страницы.

✅ Никаких действий от пользователей не требуется.

### 🧪 Тестирование

Проверьте следующие страницы:
- [ ] Каталог товаров - изображения в карточках
- [ ] Страница товара - основное изображение и галерея
- [ ] Корзина - изображения товаров
- [ ] Оформление заказа - изображения в сводке
- [ ] Поиск - миниатюры в результатах


# 🧹 Удаление водяных знаков - Быстрая инструкция

## Проблема
Изображения товаров содержат водяные знаки (antas.ru, abcp.ru, illva.ru и другие), потому что они загружены **по URL с внешних сайтов**.

## ✅ Решение
Скачать изображения локально и удалить водяные знаки с помощью Python.

---

## 🚀 Вариант 1: Простое удаление (Рекомендуется)

### Шаг 1: Установите зависимости

```bash
cd backend
pip install Pillow numpy requests beautifulsoup4
```

### Шаг 2: Запустите очистку

```bash
# Обработать первые 50 изображений (тест)
python manage.py download_and_clean_images --limit 50

# Обработать ВСЕ изображения
python manage.py download_and_clean_images --limit 1000

# Обработать конкретный товар
python manage.py download_and_clean_images --part-id 123
```

### Доступные методы:

```bash
# Метод 1: Обрезка краёв (быстро, эффективно)
python manage.py download_and_clean_images --method crop

# Метод 2: Размытие водяных знаков
python manage.py download_and_clean_images --method blur

# Метод 3: Закрашивание соседними пикселями
python manage.py download_and_clean_images --method inpaint

# Метод 4: Все методы вместе (лучший результат)
python manage.py download_and_clean_images --method all
```

---

## 🤖 Вариант 2: AI удаление (Лучшее качество)

### Шаг 1: Установите OpenCV

```bash
cd backend
pip install opencv-python opencv-contrib-python numpy
```

### Шаг 2: Запустите AI очистку

```bash
# Обработать первые 20 изображений
python manage.py ai_remove_watermarks --limit 20

# Обработать ВСЕ изображения
python manage.py ai_remove_watermarks --limit 1000

# Конкретный товар
python manage.py ai_remove_watermarks --part-id 123
```

**AI методы:**
- ✅ Детекция текстовых водяных знаков
- ✅ Умный inpainting (закрашивание)
- ✅ Умная обрезка (сохраняет главный объект)

---

## 📊 Что произойдет

### До:
```
image_url: "https://antas.ru/images/part123.jpg"  ← Внешний URL с водяным знаком
```

### После:
```
image_url: "/media/parts/5/part_5_abc123_clean.jpg"  ← Локальный файл БЕЗ водяного знака
```

### Процесс:
1. ✅ Скачает изображение с внешнего сайта
2. ✅ Откроет его с помощью PIL/OpenCV
3. ✅ Применит алгоритмы удаления водяных знаков:
   - Обрежет края (где обычно водяные знаки)
   - Размоет область водяного знака
   - Закрасит текст соседними пикселями
4. ✅ Сохранит в `backend/media/parts/`
5. ✅ Обновит URL в базе данных на локальный

---

## 🔍 Проверка результата

### 1. Проверьте в админке Django

```bash
cd backend
python manage.py runserver
```

Откройте:
- http://localhost:8000/admin/catalog/partimage/
- Посмотрите изображения - водяных знаков быть не должно!

### 2. Проверьте на frontend

```bash
cd frontend
npm run dev
```

Откройте:
- http://localhost:3000/catalog
- Посмотрите товары

### 3. Проверьте папку media

```bash
ls backend/media/parts/
```

Должны быть локальные файлы: `part_1_abc123_clean.jpg`

---

## ⚙️ Дополнительные опции

### Перезаписать существующие файлы

```bash
python manage.py download_and_clean_images --force
```

### Обработать несколько товаров

```bash
# Товары с ID от 1 до 10
for i in {1..10}; do
    python manage.py download_and_clean_images --part-id $i
done
```

### Логирование в файл

```bash
python manage.py download_and_clean_images > cleanup_log.txt 2>&1
```

---

## 🛠️ Если что-то пошло не так

### Проблема: "ModuleNotFoundError: No module named 'PIL'"

```bash
pip install Pillow
```

### Проблема: "ModuleNotFoundError: No module named 'cv2'"

```bash
pip install opencv-python opencv-contrib-python
```

### Проблема: "Не удалось скачать изображение"

Возможные причины:
- Нет интернета
- Сайт заблокирован
- Изображение удалено

**Решение:** Пропустите проблемные и обработайте остальные

### Проблема: "Водяные знаки всё ещё видны"

Попробуйте другой метод:
```bash
# Если crop не помог, попробуйте all
python manage.py download_and_clean_images --method all --force
```

Или используйте AI:
```bash
python manage.py ai_remove_watermarks
```

---

## 📝 Примеры команд

### Быстрый тест (5 изображений)

```bash
cd backend
python manage.py download_and_clean_images --limit 5 --method all
```

### Обработать весь каталог

```bash
cd backend
python manage.py download_and_clean_images --limit 10000 --method all
```

### Только датчики топлива (если знаете ID товаров)

```bash
# Обработать товары 10-20
for id in $(seq 10 20); do
    python manage.py download_and_clean_images --part-id $id --method all
done
```

---

## 💡 Какой метод выбрать?

| Метод | Скорость | Качество | Когда использовать |
|-------|----------|----------|-------------------|
| `crop` | ⚡⚡⚡ Быстро | ⭐⭐⭐ Хорошо | Водяной знак по краям |
| `blur` | ⚡⚡ Средне | ⭐⭐ Средне | Полупрозрачный водяной знак |
| `inpaint` | ⚡ Медленно | ⭐⭐⭐⭐ Отлично | Текстовый водяной знак |
| `all` | ⚡ Медленно | ⭐⭐⭐⭐ Отлично | **Рекомендуется для всех** |
| AI (OpenCV) | ⚡ Медленно | ⭐⭐⭐⭐⭐ Лучшее | Сложные водяные знаки |

---

## 🎯 Рекомендуемый порядок действий

### 1. Тест на 5 изображениях

```bash
cd backend
pip install Pillow numpy requests beautifulsoup4
python manage.py download_and_clean_images --limit 5 --method all
```

### 2. Проверьте результат

```bash
# Откройте админку
python manage.py runserver
# Посмотрите: http://localhost:8000/admin/catalog/partimage/
```

### 3. Если устраивает - обработайте все

```bash
python manage.py download_and_clean_images --limit 1000 --method all
```

### 4. Если не устраивает - попробуйте AI

```bash
pip install opencv-python opencv-contrib-python
python manage.py ai_remove_watermarks --limit 1000
```

---

## ⏱️ Сколько времени займёт?

- **5 изображений:** ~30 секунд
- **50 изображений:** ~5 минут
- **500 изображений:** ~30-60 минут
- **1000+ изображений:** ~1-2 часа

*Время зависит от скорости интернета и выбранного метода*

---

## 📞 Проблемы?

Если водяные знаки не удаляются:

1. **Попробуйте AI метод:**
   ```bash
   python manage.py ai_remove_watermarks
   ```

2. **Используйте онлайн инструменты:**
   - https://cleanup.pictures (AI удаление)
   - https://www.photopea.com (онлайн Photoshop)

3. **Сфотографируйте свои запчасти:**
   - Лучший вариант для коммерческого сайта
   - Уникальный контент без водяных знаков

---

## ✅ Итог

**Команда для запуска прямо сейчас:**

```bash
cd backend
pip install Pillow numpy requests beautifulsoup4
python manage.py download_and_clean_images --limit 50 --method all
```

После этого водяные знаки должны исчезнуть! 🎉



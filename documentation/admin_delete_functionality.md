# Функционал удаления в админ-панели

## Обзор

Добавлена возможность удаления заказов, товаров и клиентов через админ-панель с подтверждением действия.

## Безопасность

- ✅ Все операции удаления требуют аутентификации на бэкенде
- ✅ Перед удалением показывается модальное окно подтверждения
- ✅ Удаленные данные логируются в системных журналах
- ✅ Действие необратимо

## Функционал по разделам

### 1. Удаление заказов

**Расположение:** Админ панель → Заказы → Детали заказа

**Как использовать:**
1. Откройте заказ, кликнув по нему в таблице заказов
2. В модальном окне нажмите кнопку "Удалить заказ" (красная кнопка справа)
3. Подтвердите удаление в диалоговом окне

**Что происходит:**
- Заказ удаляется из базы данных
- Связанные позиции заказа удаляются автоматически (CASCADE)
- История статусов заказа удаляется автоматически
- Статистика CRM клиента обновляется автоматически
- Операция логируется с информацией о пользователе

**API Endpoint:**
```
DELETE /api/orders/{id}/
Authorization: Required
```

---

### 2. Удаление товаров

**Расположение:** Админ панель → Остатки склада → Редактировать товар

**Как использовать:**
1. Кликните по товару в таблице остатков
2. В модальном окне редактирования нажмите кнопку "Удалить" (красная)
3. Подтвердите удаление в диалоговом окне

**Что происходит:**
- Товар удаляется из каталога
- Изображения товара остаются в хранилище (можно удалить вручную)
- Если товар был в заказах, информация о нём сохраняется в позициях заказов

**API Endpoint:**
```
DELETE /api/parts/{id}/
Authorization: Required
```

**⚠️ Внимание:**
- Удаление товара не затрагивает существующие заказы с этим товаром
- Рекомендуется деактивировать товар (is_active=false) вместо удаления

---

### 3. Удаление транзакций (Касса)

**Расположение:** Админ панель → Финансы → Касса → Таблица транзакций

**Как использовать:**
1. Перейдите во вкладку "Касса"
2. Найдите транзакцию в таблице
3. Нажмите иконку корзины в колонке "Действия"
4. Подтвердите удаление в диалоговом окне

**Особенности:**
- Нельзя удалить транзакцию, связанную с заказом (сначала удалите заказ)
- После удаления автоматически пересчитывается баланс кассы
- Обновляются показатели прихода, расхода и баланса

**API Endpoint:**
```
DELETE /api/cash-transactions/{id}/
Authorization: Required
```

**Что происходит:**
- Проверка связи с заказом
- Если транзакция связана с заказом - запрос отклоняется с предупреждением
- Удаление транзакции из базы
- Автоматический пересчет баланса кассы
- Операция логируется

---

### 4. Удаление расходов

**Расположение:** Админ панель → Финансы → Расходы → Таблица расходов

**Как использовать:**
1. Перейдите во вкладку "Расходы"
2. Найдите расход в таблице
3. Нажмите иконку корзины в колонке "Действия"
4. Подтвердите удаление в диалоговом окне

**API Endpoint:**
```
DELETE /api/expenses/{id}/
Authorization: Required
```

**Что происходит:**
- Расход удаляется из базы данных
- Автоматически обновляется финансовая сводка
- Пересчитывается чистая прибыль
- Операция логируется

---

### 5. Удаление клиентов (CRM)

**Расположение:** Админ панель → Клиенты → Таблица клиентов

**Как использовать:**
1. Найдите клиента в таблице
2. Нажмите иконку корзины в колонке "Действия"
3. Подтвердите удаление в диалоговом окне

**Особенности:**
- Если у клиента есть заказы, система запросит дополнительное подтверждение
- При удалении клиента с заказами: клиент удаляется, заказы остаются в системе
- Используйте параметр `force=true` для принудительного удаления

**API Endpoint:**
```
DELETE /api/customers/{id}/?force=true
Authorization: Required
```

**Что происходит:**
1. Проверка наличия связанных заказов
2. Если заказы есть - запрос подтверждения с `force=true`
3. Удаление записи клиента из CRM
4. Заказы клиента остаются в системе (не удаляются)
5. Операция логируется

---

## Технические детали

### API функции (frontend/src/lib/utils/api.js)

```javascript
// Удаление заказа
ordersApi.deleteOrder(id)

// Удаление товара
partsApi.deletePart(id)

// Удаление транзакции
financeApi.deleteCashTransaction(id)

// Удаление расхода
financeApi.deleteExpense(id)

// Удаление клиента
crmApi.deleteCustomer(id, force = false)
```

### Компоненты с функционалом удаления

1. **OrderDetailModal.svelte** - модальное окно деталей заказа
2. **ProductEditModal.svelte** - модальное окно редактирования товара
3. **finance/+page.svelte** - страница финансов (транзакции и расходы)
4. **customers/+page.svelte** - страница управления клиентами

### Логирование

Все операции удаления логируются в backend с информацией:
- ID и название удаляемого объекта
- Пользователь, выполнивший операцию
- Временная метка
- Дополнительная информация (количество связанных объектов, суммы и т.д.)

Пример лога:
```
WARNING: Удаление заказа #GD20251110120000ABC123 | 
Клиент: +79001234567 | 
Позиций: 5 | 
Сумма: 12500.00 ₽ | 
Пользователь: admin
```

---

## Рекомендации

### Когда использовать удаление:

✅ **Заказы:**
- Тестовые/демо заказы
- Дублирующиеся записи
- Ошибочно созданные заказы
- Очистка старых данных при необходимости

✅ **Товары:**
- Тестовые товары
- Полностью неактуальные позиции
- Дубликаты

✅ **Транзакции:**
- Ошибочно созданные записи
- Тестовые транзакции
- Дублирующиеся платежи

✅ **Расходы:**
- Ошибочно внесенные расходы
- Тестовые записи
- Дубликаты

✅ **Клиенты:**
- Тестовые записи
- Дубликаты клиентов
- По запросу клиента (GDPR)

### Альтернативы удалению:

📌 **Вместо удаления товара:** установите `is_active = false`
📌 **Вместо удаления заказа:** измените статус на "Отменён"
📌 **Вместо удаления клиента:** используйте категорию "Неактивные"

---

## Безопасность и восстановление

### Защита от случайного удаления:
1. ✅ Модальное окно подтверждения
2. ✅ Требование аутентификации
3. ✅ Предупреждение о необратимости действия
4. ✅ Логирование всех операций

### Восстановление данных:
- Функция восстановления в админ-панели отсутствует
- Восстановление возможно только из резервных копий базы данных
- Рекомендуется настроить автоматическое резервное копирование

### Рекомендации по резервному копированию:
```bash
# Создать резервную копию БД
docker-compose exec db pg_dump -U user gooddrive > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановить из резервной копии
docker-compose exec -T db psql -U user gooddrive < backup_20251110_120000.sql
```

---

## Тестирование

### Чеклист перед использованием в продакшене:

- [ ] Проверить права доступа в настройках Django
- [ ] Настроить резервное копирование БД
- [ ] Протестировать удаление на тестовых данных
- [ ] Проверить логирование операций
- [ ] Убедиться в корректной работе модальных окон
- [ ] Проверить поведение при наличии связанных данных

---

## Поддержка

При возникновении проблем проверьте:
1. Авторизацию пользователя в админ-панели
2. Логи backend (docker logs gooddrive-backend-1)
3. Консоль браузера на наличие ошибок JavaScript
4. Права доступа к API endpoints

---

**Версия документа:** 1.0  
**Дата создания:** 12.11.2024  
**Автор:** GoodDrive Development Team


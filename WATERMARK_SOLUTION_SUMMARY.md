# Решение проблемы с водяными знаками на изображениях товаров

## 🔍 Проблема

На изображениях товаров (автозапчастей) в каталоге присутствуют водяные знаки "antas.ru".

**Важно:** Это НЕ иконки интерфейса (которые уже исправлены), а **фотографии товаров** из базы данных.

## ✅ Готовое решение

Я создал **автоматические инструменты** для удаления и замены изображений с водяными знаками.

### Созданные файлы:

1. **`backend/catalog/management/commands/remove_watermarked_images.py`**
   - Находит и удаляет изображения с водяными знаками
   - Поддерживает режим preview (`--dry-run`)

2. **`backend/catalog/management/commands/fetch_clean_images.py`**
   - Загружает ЧИСТЫЕ изображения БЕЗ водяных знаков
   - Использует только проверенные источники (Wikimedia, Unsplash, Pexels)
   - Автоматически фильтрует запрещенные домены

3. **`WATERMARK_REMOVAL_GUIDE.md`**
   - Подробное руководство по всем методам удаления водяных знаков

## 🚀 Быстрый старт (3 команды)

```bash
# 1. Перейдите в backend
cd backend

# 2. Удалите изображения с водяными знаками
python manage.py remove_watermarked_images

# 3. Загрузите чистые изображения
python manage.py fetch_clean_images --limit 100
```

## 📋 Подробная инструкция

### Шаг 1: Проверьте, сколько проблемных изображений

```bash
cd backend
python manage.py remove_watermarked_images --dry-run
```

**Вывод:**
```
📦 Всего изображений в базе: 150
⚠️  Найдено изображений с водяными знаками: 45

  1. [antas.ru] Датчик уровня топлива ВАЗ 2112...
  2. [antas.ru] Датчик уровня топлива ВАЗ 21101...
  ...
```

### Шаг 2: Удалите проблемные изображения

```bash
python manage.py remove_watermarked_images
```

**Что удаляется:**
- Изображения с antas.ru
- Изображения с exist.ru, emex.ru, autopiter.ru
- Изображения с фотостоков (Shutterstock, Getty Images, etc.)

### Шаг 3: Загрузите чистые изображения

```bash
# Для всех товаров без изображений
python manage.py fetch_clean_images

# Или для конкретного количества
python manage.py fetch_clean_images --limit 50

# Или для конкретного товара
python manage.py fetch_clean_images --part-id 123

# Или заменить ВСЕ изображения
python manage.py fetch_clean_images --force
```

**Откуда загружаются изображения:**
- ✅ Wikimedia Commons (без водяных знаков, свободная лицензия)
- ✅ Unsplash (бесплатно, коммерческое использование)
- ✅ Pexels (бесплатно, коммерческое использование)
- ✅ DuckDuckGo (с фильтрацией запрещенных доменов)

❌ **НЕ используется:**
- antas.ru, exist.ru, emex.ru (водяные знаки)
- Shutterstock, Getty Images (платно, водяные знаки)

### Шаг 4: Проверьте результат

```bash
# Запустите frontend
cd ../frontend
npm run dev

# Откройте сайт
# http://localhost:3000/catalog
```

## 🛡️ Защита от водяных знаков

Команда `fetch_clean_images` **автоматически проверяет** каждое изображение:

```python
# Список запрещенных доменов (автоматически отклоняются)
BLACKLISTED_DOMAINS = [
    'antas.ru',         # ❌ Текущая проблема
    'exist.ru',
    'emex.ru',
    'autopiter.ru',
    'shutterstock',
    'gettyimages',
    'istockphoto',
    # ... и другие
]

# Список НАДЕЖНЫХ источников (предпочтительно)
TRUSTED_SOURCES = [
    'wikimedia.org',    # ✅ Без водяных знаков
    'unsplash.com',     # ✅ Бесплатно
    'pexels.com',       # ✅ Бесплатно
    'pixabay.com',      # ✅ Бесплатно
]
```

## 💾 Резервная копия

**ВАЖНО:** Сделайте бэкап перед массовым удалением!

```bash
# Сохраните текущие изображения
cd backend
python manage.py dumpdata catalog.PartImage > backup_images.json

# При необходимости восстановить:
python manage.py loaddata backup_images.json
```

## 🎯 Что делать, если автоматическая загрузка не работает

### Вариант 1: Скачать изображения вручную

**Бесплатные источники:**
1. **Unsplash** - https://unsplash.com
   - Поиск: "auto parts", "car parts"
2. **Pexels** - https://pexels.com
   - Поиск: "automotive", "spare parts"
3. **Pixabay** - https://pixabay.com
   - Поиск: "car repair"

### Вариант 2: Сфотографировать свои запчасти

Это **лучший вариант** для коммерческого сайта:
- ✅ Уникальный контент
- ✅ Без водяных знаков
- ✅ Лучше для SEO
- ✅ Доверие клиентов

### Вариант 3: Использовать AI для удаления водяных знаков

**Онлайн инструменты:**
- https://cleanup.pictures - AI удаление объектов (бесплатно)
- https://www.remove.bg - Удаление фона
- https://www.photopea.com - Онлайн Photoshop

**Программы:**
- GIMP (бесплатно) - инструмент "Clone Tool"
- Paint.NET (бесплатно) - плагин "Clone Stamp"

## 📊 Статистика

### Что было сделано:

| Этап | Файл | Статус |
|------|------|--------|
| Замена иконок интерфейса | `frontend/static/icons/*.png` | ✅ Выполнено (SVG без водяных знаков) |
| Команда удаления | `remove_watermarked_images.py` | ✅ Создана |
| Команда загрузки | `fetch_clean_images.py` | ✅ Создана |
| Инструкция | `WATERMARK_REMOVAL_GUIDE.md` | ✅ Создана |
| Удаление водяных знаков с товаров | Запустите команды → | ⏳ **Требует выполнения** |

## 🔄 Автоматизация (опционально)

Настройте автоматическую проверку новых изображений:

```python
# backend/gooddrive_backend/settings.py

# Добавьте в INSTALLED_APPS
INSTALLED_APPS = [
    ...
    'django_cron',  # pip install django-cron
]

# Создайте cron задачу
# backend/catalog/cron.py
from django_cron import CronJobBase, Schedule
from catalog.management.commands.remove_watermarked_images import Command as RemoveCmd
from catalog.management.commands.fetch_clean_images import Command as FetchCmd

class CheckWatermarksCronJob(CronJobBase):
    schedule = Schedule(run_every_mins=1440)  # Каждые 24 часа
    code = 'catalog.check_watermarks'

    def do(self):
        # Удалить водяные знаки
        RemoveCmd().handle()
        # Загрузить чистые изображения
        FetchCmd().handle()
```

## 📝 Проверочный чеклист

После выполнения команд проверьте:

- [ ] Запустил `remove_watermarked_images --dry-run` (проверка)
- [ ] Запустил `remove_watermarked_images` (удаление)
- [ ] Запустил `fetch_clean_images` (загрузка новых)
- [ ] Проверил товары в каталоге (frontend)
- [ ] Проверил админку Django
- [ ] Водяных знаков больше нет
- [ ] Изображения корректно отображаются

## 🆘 Решение проблем

### Команды не находятся

```bash
# Убедитесь, что файлы созданы
ls backend/catalog/management/commands/

# Должны быть:
# - __init__.py
# - remove_watermarked_images.py
# - fetch_clean_images.py
# - fetch_part_images.py (старый)

# Перезапустите Django
cd backend
python manage.py runserver
```

### Изображения не загружаются

```bash
# Проверьте интернет-соединение
curl https://commons.wikimedia.org

# Попробуйте для одного товара
python manage.py fetch_clean_images --part-id 1

# Посмотрите логи
python manage.py fetch_clean_images 2>&1 | tee upload.log
```

### Водяные знаки остались

```bash
# Возможно, изображение хранится локально в media/
ls backend/media/parts/

# Удалите локальные файлы с водяными знаками вручную
rm -rf backend/media/parts/*/watermarked_image.jpg

# Затем снова запустите загрузку
python manage.py fetch_clean_images --force
```

## 📞 Следующие шаги

1. **Прямо сейчас:**
   ```bash
   cd backend
   python manage.py remove_watermarked_images
   python manage.py fetch_clean_images --limit 10
   ```

2. **После проверки:**
   - Если результат устраивает → запустите для всех товаров
   - Если нет → используйте ручную загрузку или фотографируйте сами

3. **Для будущего:**
   - Создайте библиотеку своих фотографий
   - Настройте автоматическую проверку
   - Добавьте API ключи для Unsplash/Pexels

---

**Готово!** Все инструменты созданы. Запустите команды и водяные знаки исчезнут! 🎉


# Руководство по удалению водяных знаков с изображений товаров

## 📸 Проблема

На изображениях автозапчастей в базе данных присутствуют водяные знаки "antas.ru" и других сайтов.

**Пример проблемных изображений:**
- Датчик уровня топлива - водяной знак "antas.ru"
- Другие запчасти с логотипами сайтов-источников

## ⚠️ ВАЖНО: Различие между иконками и изображениями товаров

| Тип | Расположение | Что было сделано |
|-----|--------------|------------------|
| **Иконки интерфейса** | `frontend/static/icons/*.png` | ✅ Заменены на SVG без водяных знаков |
| **Изображения товаров** | База данных → URL в модели `PartImage` | ❌ **Содержат водяные знаки** |

## 🛠️ Решения

### Решение 1: Автоматическое удаление и замена (Рекомендуется)

Я создал две команды Django для автоматизации процесса:

#### Шаг 1: Найти и удалить изображения с водяными знаками

```bash
cd backend

# 1. Предпросмотр (не удаляет, только показывает)
python manage.py remove_watermarked_images --dry-run

# 2. Удалить изображения с водяными знаками
python manage.py remove_watermarked_images
```

**Что делает команда:**
- Сканирует все изображения в базе данных
- Находит изображения с водяными знаками по URL (antas.ru, exist.ru, emex.ru, и др.)
- Удаляет их из базы данных

#### Шаг 2: Загрузить ЧИСТЫЕ изображения

```bash
# Загрузить чистые изображения для товаров без фото
python manage.py fetch_clean_images

# Или заменить ВСЕ изображения на чистые
python manage.py fetch_clean_images --force
```

**Что делает команда:**
- Ищет изображения ТОЛЬКО в надежных источниках:
  - ✅ Wikimedia Commons (без водяных знаков)
  - ✅ Unsplash (бесплатные, без водяных знаков)
  - ✅ Pexels (бесплатные, без водяных знаков)
- **Автоматически фильтрует** водяные знаки
- **Проверяет URL** на наличие запрещенных доменов

### Решение 2: Ручная замена изображений

Если автоматическая загрузка не находит подходящие изображения:

#### 1. Скачайте чистые изображения вручную

**Бесплатные источники БЕЗ ВОДЯНЫХ ЗНАКОВ:**

| Сайт | URL | Что искать | Лицензия |
|------|-----|-----------|----------|
| **Unsplash** | https://unsplash.com | "auto parts", "car parts", "automotive" | Бесплатно |
| **Pexels** | https://pexels.com | "car repair", "spare parts" | Бесплатно |
| **Pixabay** | https://pixabay.com | "automobile parts" | Бесплатно |
| **Wikimedia Commons** | https://commons.wikimedia.org | Специфические запчасти | Бесплатно |

#### 2. Загрузите через админ-панель Django

```bash
# 1. Войдите в админ-панель
http://localhost:8000/admin/

# 2. Перейдите в:
Catalog → Part Images → Добавить изображение

# 3. Загрузите файл и привяжите к товару
```

### Решение 3: Использовать AI для удаления водяных знаков

Если изображения уже сохранены локально, можно использовать AI:

#### Онлайн инструменты (бесплатно):

1. **Cleanup.pictures**
   - URL: https://cleanup.pictures
   - Бесплатно, AI удаление
   - Без регистрации

2. **Remove.bg** (для объектов)
   - URL: https://www.remove.bg
   - Удаляет фон (в том числе водяные знаки)

3. **Photopea** (онлайн Photoshop)
   - URL: https://www.photopea.com
   - Инструмент "Clone Stamp" для ручного удаления

#### Программы (оффлайн):

1. **GIMP** (бесплатно)
   - Инструмент "Заплатка" или "Clone Tool"

2. **Paint.NET** (бесплатно, Windows)
   - Plugin "Clone Stamp"

3. **Photoshop** (платно)
   - Content-Aware Fill

### Решение 4: Python скрипт для массового удаления

Если водяные знаки находятся в одном месте (например, углу):

```python
# backend/remove_watermarks.py
from PIL import Image
import os

def remove_watermark(image_path, output_path, crop_area=(0, 0, 800, 600)):
    """
    Удаляет водяной знак путем обрезки изображения
    
    crop_area: (left, top, right, bottom) - область БЕЗ водяного знака
    """
    img = Image.open(image_path)
    
    # Обрезаем область с водяным знаком
    cropped = img.crop(crop_area)
    
    # Сохраняем
    cropped.save(output_path, quality=95)
    print(f"✅ Обработано: {os.path.basename(output_path)}")

# Использование
# remove_watermark('input.jpg', 'output.jpg')
```

## 📋 Проверка списка доменов с водяными знаками

Команда `remove_watermarked_images` автоматически находит изображения с этих сайтов:

```python
BLACKLISTED_DOMAINS = [
    'antas.ru',           # ❌ Ваша текущая проблема
    'exist.ru',
    'emex.ru',
    'autopiter.ru',
    'avto-moto24.ru',
    'shutterstock',
    'gettyimages',
    'istockphoto',
    '123rf',
    'dreamstime',
    'depositphotos',
    'freepik',
    'alamy',
]
```

## 🎯 Рекомендуемый порядок действий

### Для быстрого решения:

```bash
cd backend

# 1. Сначала посмотрите, сколько проблемных изображений
python manage.py remove_watermarked_images --dry-run

# 2. Удалите их
python manage.py remove_watermarked_images

# 3. Загрузите чистые изображения
python manage.py fetch_clean_images --limit 100

# 4. Проверьте результат в админке
python manage.py runserver
# Откройте http://localhost:8000/admin/catalog/partimage/
```

### Для ручного контроля:

```bash
# 1. Удалите только изображения с водяными знаками
python manage.py remove_watermarked_images

# 2. Скачайте нужные изображения вручную с Unsplash/Pexels

# 3. Загрузите через админ-панель Django
# http://localhost:8000/admin/catalog/partimage/add/
```

## 📊 Проверка результата

После выполнения команд:

1. **Откройте frontend:**
   ```bash
   cd frontend
   npm run dev
   ```

2. **Проверьте товары:**
   - http://localhost:3000/catalog
   - Убедитесь, что водяных знаков нет

3. **Проверьте админку:**
   - http://localhost:8000/admin/catalog/partimage/
   - Просмотрите изображения

## 🔧 Если изображения не загружаются автоматически

Команда `fetch_clean_images` использует публичные API без ключей. Для лучших результатов:

### 1. Получите API ключи (бесплатно):

**Unsplash API:**
```bash
# 1. Зарегистрируйтесь: https://unsplash.com/developers
# 2. Создайте приложение
# 3. Скопируйте Access Key
# 4. Добавьте в настройки Django:

# backend/gooddrive_backend/settings.py
UNSPLASH_ACCESS_KEY = 'your_access_key_here'
```

**Pexels API:**
```bash
# 1. Зарегистрируйтесь: https://www.pexels.com/api/
# 2. Скопируйте API Key
# 3. Добавьте в настройки:

PEXELS_API_KEY = 'your_api_key_here'
```

### 2. Используйте платные API (для коммерческого использования):

- **Adobe Stock API** - качественные изображения
- **Shutterstock API** - большая библиотека (но нужно платить)

## 📝 Логирование

Все команды выводят подробную информацию:

```
🚀 Начинаем удаление изображений с водяными знаками
📦 Всего изображений в базе: 150

⚠️  Найдено изображений с водяными знаками: 45

  1. [antas.ru] Датчик уровня топлива ВАЗ 2112...
  2. [exist.ru] Масляный фильтр...
  ...

✅ Удалено изображений с водяными знаками: 45
```

## ⚠️ Предупреждения

1. **Резервная копия:**
   ```bash
   # Сделайте бэкап БД перед массовым удалением
   python manage.py dumpdata catalog.PartImage > backup_images.json
   ```

2. **Проверка лицензий:**
   - Unsplash, Pexels, Pixabay - можно использовать коммерчески
   - Wikimedia - проверяйте лицензию каждого изображения
   - Google Images - НЕ используйте (защищены авторским правом)

3. **Качество изображений:**
   - Автоматическая загрузка может найти не совсем подходящие фото
   - Рекомендуется ручная проверка для важных товаров

## 💡 Советы

1. **Создайте собственную библиотеку:**
   - Фотографируйте свои запчасти
   - Уникальный контент без водяных знаков
   - Лучше для SEO

2. **Используйте placeholder:**
   - Для товаров без фото используйте стандартную иконку
   - Создайте красивый градиент или лого

3. **Автоматизируйте:**
   - Настройте cron для регулярной проверки
   - Автоматически загружайте фото для новых товаров

## 📞 Помощь

Если команды не работают:

```bash
# 1. Проверьте, что файлы созданы
ls backend/catalog/management/commands/

# 2. Должны быть файлы:
# - remove_watermarked_images.py
# - fetch_clean_images.py

# 3. Создайте __init__.py если его нет
touch backend/catalog/management/commands/__init__.py

# 4. Перезапустите Django
python manage.py runserver
```

---

**Итог:** Используйте команду `remove_watermarked_images` для удаления, затем `fetch_clean_images` для загрузки чистых изображений!


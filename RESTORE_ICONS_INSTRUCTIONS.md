# Инструкция по восстановлению PNG иконок

## ⚠️ Проблема

PNG файлы иконок были удалены из папки `frontend/static/icons/`:
- ❌ `brake_ic.png` - удален
- ❌ `electronics_ic.png` - удален
- ❌ `engine_ic.png` - удален
- ❌ `suspension_ic.png` - удален
- ❌ `shoping_cart.png` - удален

Код откачен к использованию PNG иконок, но сами файлы отсутствуют.

## ✅ Решения

### Вариант 1: Восстановить из Git (Если используете Git)

Если вы используете Git и файлы были закоммичены:

```bash
cd "C:\Users\Daniil Arkhipov\Desktop\Avtodeleer\GoodDrive"

# Проверьте историю
git log --oneline -- frontend/static/icons/

# Восстановите файлы из последнего коммита, где они были
git checkout HEAD~1 -- frontend/static/icons/
```

### Вариант 2: Восстановить из резервной копии

Если у вас есть резервная копия проекта:
1. Найдите папку с бэкапом
2. Скопируйте файлы из `frontend/static/icons/` в текущий проект

### Вариант 3: Скачать новые иконки

Скачайте бесплатные PNG иконки с этих сайтов:

#### 1. **Flaticon** (бесплатно с атрибуцией)
https://www.flaticon.com/

**Что искать:**
- `brake` или `brake disc` → для brake_ic.png
- `circuit board` или `electronics` → для electronics_ic.png
- `engine` или `motor` → для engine_ic.png
- `suspension` или `shock absorber` → для suspension_ic.png
- `shopping cart` → для shoping_cart.png

#### 2. **Icons8** (бесплатно)
https://icons8.com/icons

#### 3. **Iconfinder** (есть бесплатные)
https://www.iconfinder.com/

### Вариант 4: Создать простые иконки

Если нужны быстрые заглушки, можно создать простые одноцветные иконки:

1. Зайдите на https://www.canva.com
2. Создайте квадратное изображение 128x128px
3. Используйте простые формы и иконки
4. Экспортируйте как PNG

## 📏 Требования к иконкам

Для правильного отображения на сайте:

| Параметр | Значение |
|----------|----------|
| Формат | PNG с прозрачностью |
| Размер | 64x64px - 256x256px |
| Цвет | Черный/темный (цвет меняется через CSS фильтры) |
| Фон | Прозрачный |
| Стиль | Минималистичный, контурный |

## 📂 Куда поместить файлы

Скачанные иконки нужно поместить в:
```
frontend/static/icons/
├── brake_ic.png
├── electronics_ic.png
├── engine_ic.png
├── suspension_ic.png
└── shoping_cart.png
```

## 🎨 CSS фильтры для цвета

Иконки будут автоматически перекрашены через CSS:
- Обычное состояние: бордовый цвет (`.icon-maroon`)
- При hover: белый цвет (`.group:hover .icon-maroon`)

Поэтому **не важно, какого цвета будет исходная иконка** (лучше черная или темная).

## 🚀 После добавления файлов

1. Убедитесь, что все 5 PNG файлов находятся в `frontend/static/icons/`
2. Перезапустите сервер разработки:
   ```bash
   cd frontend
   npm run dev
   ```
3. Откройте http://localhost:3000 и проверьте иконки

## 💡 Альтернатива: Использовать SVG вместо PNG

Если не хотите искать PNG файлы, можно использовать Lucide Icons (SVG), которые уже настроены и работают:

1. Откатите изменения обратно к Lucide
2. Они не требуют файлов изображений
3. Лучшее качество и производительность

**Команда для отката к Lucide:**
```bash
git checkout HEAD -- frontend/src/
```

## 📞 Нужна помощь?

Если не получается восстановить иконки, дайте знать, и я:
- Помогу настроить Git для восстановления
- Подскажу лучшие бесплатные иконки
- Настрою использование SVG вместо PNG

